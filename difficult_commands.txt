echo hello world > $a # expand $a to filename
export $a=value # invalid key
export key=$a # expand $a
export key="$a" # expand $a and remove quotes
export key='$a' # expand $a and remove quotes
cat -e <<a <<b # first close a, then b; b overwrites a
<a cat -e > a_out | <b cat -e > b_out # a -> a_out; b -> b_out; nothing is piped
<<a cat -e > a_out | <<b cat -e > b_out # herdoc:a -> a_out; heredoc:b -> b_out; nothing is piped
<c <b cat -e # c: no such file or directory
directory/executable # such be treated as relative path